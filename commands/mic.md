---
name: mic
description: "Mikrofona konuş, Whisper ile Türkçe transkript al, clipboard'a kopyala. Triggers: mic, mikrofon, ses, konuş, dikte, sesli giriş"
---

# /mic — Sesli Giriş (Türkçe)

```bash
bash ~/.claude/plugins/voice-input/scripts/voice.sh
```
